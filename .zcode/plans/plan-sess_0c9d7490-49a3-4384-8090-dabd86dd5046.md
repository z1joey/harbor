# harbor-pilot：改名 + 完全独立定位

## 前提

上一轮改造后 skill 在技术上已不依赖 app（`register_project.py` 自己创建 `~/.harbor`，端口规划读磁盘 + 实时 lsof）。这次把**独立运行转正为明确定位**，并完成改名。

## 1. 重命名与批量替换

- `~/.agents/skills/harbor-toml` → `~/.agents/skills/harbor-pilot`（目录即 repo，git 不受影响）
- 两个仓库所有 `harbor-toml`（带连字符）→ `harbor-pilot`；`harbor.toml` / `validate_harbor_toml.py` 等文件名（带点/下划线）**不动**。涉及：
  - Skill：`SKILL.md`（含 `name:` frontmatter）、`README.md`
  - Harbor：README、`docs/ACCEPTANCE.md`、设计 spec、5 个 SwiftUI 文案、TUI 提示、4 处 Core 注释

## 2. Skill 独立定位（SKILL.md / README 重写要点）

- 标题/简介：**Harbor Pilot — standalone**：给 agent 用的开发端口规划与注册工具；Harbor app 是*可选消费者*，不是前置条件
- 明确独立性保证：无 `~/.harbor` 时 `register_project.py` 自动创建（池缺省 8100–8199，注册表空起步）；读取路径对旧版 App Support 兜底；校验/查询永不要求 app 在运行
- 触发词保留（"接入 harbor"、"写个 harbor 配置"等），description 说明"with or without the Harbor app running"

## 3. Harbor 主仓库引用同步

已含在上面的批量替换里；替换后 grep 复查两个仓库 `harbor-toml` 归零。

## 4. 验证

- Skill：`python3 scripts/test_port_pool.py`（28 项）
- Harbor：`swift test`（98 项）+ `xcodegen generate` + `xcodebuild` Debug
- 重建并重启 app，确认 UI 新文案（popover 空状态/错误条幅指向 harbor-pilot）

## 5. 提交

- Skill repo：`harbor-home-dir` 分支追加 commit（改名 + 独立定位）
- Harbor repo：同分支追加 commit（引用同步）
- **不改 GitHub 远端**（外向操作，留给你）：`gh repo rename harbor-pilot -R z1joey/harbor-toml`，然后本地 `git remote set-url origin …/harbor-pilot.git`（旧 URL GitHub 会自动重定向，不急）
